# Runtime dependencies for the single pledges-API Lambda (FastAPI + Mangum).
# boto3 is provided by the Lambda runtime, so it is not listed here. CDK bundles
# these into the function asset at deploy (see cdk/src/constructs/lambdas.py).
fastapi>=0.115,<0.116
# Mangum >=0.21 for Python 3.14: older versions call asyncio.get_event_loop()
# with no running loop, which raises RuntimeError on 3.14 (the auto-create
# behavior was removed). 0.21 sets up the loop first (R2).
mangum>=0.21,<0.22
